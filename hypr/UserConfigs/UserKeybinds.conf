# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# This is where you put your own keybinds. Be Mindful to check as well ~/.config/hypr/configs/Keybinds.conf to avoid conflict
# if you think I should replace the Pre-defined Keybinds in ~/.config/hypr/configs/Keybinds.conf , submit an issue or let me know in DC and present me a valid reason as to why, such as conflicting with global shortcuts, etc etc

# See https://wiki.hyprland.org/Configuring/Keywords/ for more settings and variables
# See also Laptops.conf for laptops keybinds 

# /* ---- ✴️ Variables ✴️ ---- */  #
$mainMod = SUPER
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$UserConfigs = $HOME/.config/hypr/UserConfigs

#  IMPORTANT: If you want to remap and existing keybind you MUST unbindd it first 

# The bindings are CASE SENSITIVE. We suggest you copy the exisitng binding here
#  Then change `bindd` to `unbind`

# E.g. 
# unbind = $mainMod, Return, Open terminal, exec, $term
# bindd = $mainMod, Return, Open terminal, exec, ghostty 
#
# unbind = $mainMod, E, file manager, exec, $files
# bindd = $mainMod, T, file manager, exec, $files

# If you are ADDING a bindd, make sure you include the description 
# Other the keybind search menu might not show it properly 

# E.g.
# bindd = $mainMod, Z, My z app, exec APPNAME

##############
#  unbinds   #
##############
# Unbind default keys that conflict with our new bindings
unbind = $mainMod, SPACE
unbind = $mainMod, D
unbind = $mainMod, Return
unbind = $mainMod, T
unbind = ALT, Tab
unbind = $mainMod, Tab
unbind = $mainMod, A

##############
#  Rebinds   #
##############
# App launcher
bindd = $mainMod, SPACE, App launcher, exec, pkill rofi || true; $scriptsDir/RofiFocusedWallpaperLink.sh >/dev/null 2>&1 || true; rofi -show drun -modi drun,filebrowser,run,window

# Terminal
bindd = $mainMod, T, Open terminal, exec, $scriptsDir/LaunchTerminal.sh "$term"

# Switch workspace (next/cycle)
bindd = ALT, Tab, Next workspace, workspace, m+1

# Workspace overview
bindd = $mainMod, Tab, Desktop overview, exec, $scriptsDir/OverviewToggle.sh

# Clipboard manager
unbind = $mainMod, V
bindd = $mainMod, V, Clipboard manager, exec, copyq toggle

# Toggle floating window
unbind = ALT, SPACE
bindd = ALT, SPACE, Toggle floating window, togglefloating,

# Screenshots
unbind = , Print
unbind = CTRL, Print
unbind = SHIFT, Print
bindd = , Print, Custom screenshot area, exec, $UserScripts/CustomScreenshot.sh area
bindd = CTRL, Print, Custom screenshot active window, exec, $UserScripts/CustomScreenshot.sh active
bindd = SHIFT, Print, Custom screenshot screen, exec, $UserScripts/CustomScreenshot.sh screen

# Power and Lock
unbind = CTRL ALT, Delete
unbind = CTRL ALT, P
unbind = CTRL ALT, L
unbind = $mainMod, L
bindd = CTRL ALT, Delete, Powermenu, exec, $scriptsDir/Wlogout.sh
bindd = $mainMod, L, Lock screen, exec, $scriptsDir/LockScreen.sh
# For passthrough keyboard into a VM
# bind = $mainMod ALT, P, submap, passthru
#submap = passthru
# to unbind
#bind = $mainMod ALT, P, submap, reset
#submap = reset
